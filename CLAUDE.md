・要件定義、計画、レビュー、設計相談、複雑なバグ調査はOpusが行う
・実装、ファイル整理、単純作業はSonnetのサブエージェントに委任
・質問に対しては、Sonnetが答えてください。
・ローカルでコミットしたら、その都度 `git push origin main` でGitHubへも反映する(確認なしでよい)。pushしないままセッションが続くとローカルとリモートの乖離が大きくなるため、コミット後は毎回push状態を保つこと。