# フリーミアム化 実装企画書（無料版 / Pro版）

> **⚠️ この文書の「プラン別制限」は 2026-07-31 に置き換えられました。**
> 現行仕様は [`docs/superpowers/specs/2026-07-31-freemium-v2-limits-design.md`](docs/superpowers/specs/2026-07-31-freemium-v2-limits-design.md)（無料枠ユニットモデル）を参照してください。
> 本文書に出てくる「日次100件」「`FREE_DEVICE_DAILY_LIMIT`（150件）」「グラフはPro限定」は**すべて廃止済み**です。
> また「既存ユーザー」に関する記述（162行目付近）も誤りで、本アプリは未公開・利用者ゼロです。
> 制限以外の記述（StoreKit基盤・AdMob・公開前TODO）は引き続き有効です。

作成: 2026-07-14 / 最終更新: 2026-07-15 / ステータス: **Phase 1〜2 完了・Phase 3 一部完了（レシート検証は公開準備時）**

## 実装状況（2026-07-15 時点・サーバー82テスト/iOSビルド 検証済み）

- **Phase 1: 完了** — サーバーゲート(X-App-Plan) / StoreKit2基盤+ペイウォール / クライアントゲート（OCRロック・無料5秒/Pro1秒クールダウン・日次100件・オファーぼかしダミー+鍵・グラフロック）。「あと◯秒」オーバーレイも実装済み。StoreKit基盤は実機で購入→Pro反映を確認済み。
- **Phase 2: 完了** — AdMobバナー広告。※企画から変更: バナーは**320x100固定**（アダプティブではない）、無料のグラフ枠は**ぼかしダミーグラフを廃止**し「鍵+Pro案内 + 広告バナー」構成。ATT/PrivacyInfo/SKAdNetwork(2件)対応済み。
- **Phase 3: 一部完了**
  - ✅ **デバイス単位のサーバー側レート制限**（X-Device-Id、無料の /api/search を日次バックストップ。既定150件=クライアント100の上・env FREE_DEVICE_DAILY_LIMIT）。
  - ⏳ **App Store Server API によるレシート検証は未着手**（App Store Connect のアプリ・サブスク商品登録＋実トランザクションが前提のため、公開準備フェーズで実施）。
  - ⏳ **端末証明（App Attest）は未着手・将来実装予定**（詳細は下記「4.2c 端末証明（App Attest）— 将来実装予定」参照）。
  - ⏳ **アカウント（ログイン）は未着手・将来実装予定**（詳細は下記「4.2d アカウント（ログイン）— 将来実装予定」参照）。
- **共有コスト削減**: ✅ Keepa経路の検索/オファー結果を **30分キャッシュ**に延長（`KEEPA_CACHE_TTL_MS`）。SP-API経路はBYOのため既定5分のまま。

### 公開前 必須TODO（2026-08-14時点で更新。完了したものは取り消し線）

- ~~サーバー `.env` から `LWA_REFRESH_TOKEN` を外す~~ → 本番はCloudflare Workersのため`.env`自体がデプロイされない。代わりに`wrangler secret list`でSecretに登録されていないことを確認する運用に変更（詳細は4.2の該当箇所）。
- ~~バンドルID `com.example.barcodesedori` → 本番ID~~ `jp.sellira.sellerlens` に変更済み（2026-08-14）。
- ~~AdMob を本番IDへ差し替え~~ アプリ側（`GADApplicationIdentifier`）・サーバー側KVとも本番ID取得・設定済み。**ただし開発中は動作確認のためKVをテストIDへ戻してある**（未公開アプリの本番ユニットはNo Fillで広告が出ないため）。本番IDと切り替え手順はSESSION-HANDOFF.mdの「AdMob本番IDと切り替え運用」に記録済み。
- ~~PrivacyInfo の TrackingDomains 記載~~ 実測して記載済み（2026-08-14）。**ただし`PrivacyInfo.xcprivacy`が長期間アプリバンドルに含まれていない不具合があり、この記載も含めて何も効いていなかった**。`project.yml`の`sources:`への個別ファイル指定で修正済み。SESSION-HANDOFF.mdの「プライバシーマニフェスト」参照。
- ~~試験用の手動SP-APIキー入力欄を削除~~ 対応済み（`SecureField`は現在Keepa BYOキー欄のみで、これは意図通り）。
- **PostHog**: APIキー設定済み。プライバシーポリシーへの記載、App Store Connectのプライバシー表示申告は**未対応のまま**（下記参照）。
- **プライバシーポリシーのページを `https://sellira.jp/privacy/` に公開する**（別リポジトリ sellira-site）。ページ自体は存在確認済み（2026-08-13）だが、**本番デプロイ済みかは未確認**。課金画面から直接リンクしており、未公開だと審査に落ちる。
- **App Store アプリIDを `SettingsView.swift` の `AppStoreReviewConfig.appId` に設定する**（現在は空文字）。**App Store Connectでのアプリ登録が前提**。空の間は設定タブの「レビューを書く」行が非表示のまま公開されてしまう。
- ~~SP-API refresh token を UserDefaults → Keychain へ移行~~ 対応済み。**あわせて出品者ID(sellerId)もKeychainへ移行済み**（2026-08-14。再インストールで連携が壊れるバグの修正。SESSION-HANDOFF.md参照）。
- （公開前・偽装対策・未対応）自己申告 X-App-Plan を廃し、App Store Server API レシート検証を導入。
- **App Store Connectのプライバシー申告（Webフォーム）を提出時に記入する**。`PrivacyInfo.xcprivacy`はアプリ自身のコード分のみでよい（AdMob/PostHog等SDK分は各SDKが自前で申告済みのためマニフェストへの追記は不要）が、**Webフォームの方はSDK分も含めて申告する必要がある**。AdMobの申告内容はGoogleが公開: <https://developers.google.com/admob/ios/privacy/data-disclosure>。詳細はSESSION-HANDOFF.mdの「マニフェストとApp Store Connectのプライバシー申告の使い分け」参照。
- **無認証で公開されているデモ用エンドポイント2本**（`/api/keepa-throttle-demo/seed` `/probe`）にIPレート制限を追加済み（2026-08-14）。エンドポイント自体は本番検証用に維持する判断（削除しない）。
- リワード広告のSSVコールバックURL（`https://api.sellira.jp/api/admob/ssv`）をAdMobコンソールで設定する（ユーザー作業・未対応）。
- **忘れずに: Xcodeでアーカイブ（配布用ビルド）を作る際は必ずReleaseビルド構成にすること。** DEBUG限定の開発者向け機能（`EntitlementStore`のPro強制フラグ、Keepaスロットルのデモ機能等）は`#if DEBUG`で確実に除外されるため、Release構成でアーカイブすれば混入の心配はない（Debug構成のままアーカイブしようとするとXcodeが警告する）。Xcode Organizerでの「Generate Privacy Report」もこのタイミングで確認する（Instrumentsの警告はAppleの既知バグで信頼できないため）。

## 1. 目的

- 無料版で体験を提供しつつ、広告収入とPro課金で収益化する。
- SP-API / Keepa の従量コスト（トークン・レート制限）を無料ユーザーから守る。

## 2. プラン定義（機能マトリクス）

**重要な前提**: SP-APIは**利用者自身のセラーアカウントをOAuthで接続（BYO）**して使う。レート枠はセラー単位のため利用者間で干渉せず、SP-APIのオファーは第1段階（/api/search）に同梱済み＝**追加通信ゼロ**。よって無料でもSP-API接続者にはオファーを開放する。守るべき共有コストは**サーバーのKeepa鍵**のみ。

| 機能 | 無料版 | Pro版（課金） |
|---|---|---|
| バーコードスキャン | ○（**5秒に1回**まで） | ○（現行1秒クールダウン） |
| 1日のスキャン上限 | **100件/日** | 無制限 |
| OCRスキャン（ISBN/JAN文字認識） | △（**無料は1日3回まで**お試し可。超過で鍵表示→ペイウォール） | ○（無制限） |
| SP-API接続（自分のセラーをOAuth接続=BYO） | ○ | ○ |
| 第1段階: 簡易価格（タイトル/画像/新品・中古最安） | ○（SP-API接続時はSP-API、未接続はKeepa） | ○ |
| 第2段階: オファー一覧（コンディション・送料込・最安順） | **SP-API接続時のみ ○**（第1段階に同梱）。Keepaのみ(未接続)は×（鍵→タップで設定タブ=Amazon連携へ誘導） | **同左（Proでも SP-API連携が必須）**。Keepa第2段階のトークン消費を避けるため |
| Keepaグラフ | ×（**ぼかしグラフ＋鍵＋広告バナー**） | ○ |
| 広告 | あり（グラフ枠に表示） | なし |
| スキャン履歴保存 | 無制限（制限なし） | 無制限 |
| 損益分岐点（breakEven表示） | ○（SP-API接続時。各自の枠で算出＝共有コストなし） | ○ |

無料層は実質2種類に分かれる:
- **BYO-SP-API接続者**（実セラー）: 価格＋オファー一覧＋breakEven まで見られる（各自の枠）。Pro差分は OCR / Keepaグラフ / 広告なし / スキャン速度・日次無制限。
- **Keepaのみ（未接続）**: 第1段階の簡易価格まで。オファーは鍵＋ぼかしダミー、グラフはロック。

補足（コンバージョン設計）:
- グラフ枠は「広告だけ」より「ロックされた価値が見える」方が課金転換率が高いため、**ダミー/ぼかしグラフ＋鍵アイコン＋その下に広告** の構成とする。
- **Keepa経路のオファー**（＝SP-API未接続の無料ユーザー）は**実データを一切取得しない**（Keepa第2段階=offers付きgetProductはトークン消費が大きいため）。代わりに**ハードコードのダミーオファー数行にぼかし（blur）＋鍵アイコン**を重ねて表示し、タップでペイウォールへ。ダミーなので**サーバーのKeepaトークン消費はゼロ**。
- 一方 **SP-API経路のオファーは第1段階に同梱済み**なので、無料でもそのまま実データを表示する（追加コストなし）。

## 3. 課金方式

- **月額サブスクリプション（StoreKit 2 / 自動更新）**。買い切りは提供しない。
  - 理由: SP-API/Keepaの利用コストが毎月発生するため、買い切りだとヘビーユーザーが恒常的な赤字要因になる。
- **価格: ¥1,980/月**。**無料トライアル: 3日間**（導入オファー）。
- 必須実装: 購入 / 復元（Restore Purchases）/ 期限切れ検知 / ペイウォール画面。

## 4. アーキテクチャ方針

### 4.1 エンタイトルメント（プラン状態）の持ち方

- iOS: `EntitlementStore`（新規, ObservableObject）を追加。StoreKit 2 の `Transaction.currentEntitlements` を起動時+変更時に監視し、`isPro: Bool` を公開。
- 全ゲートはこの `isPro` を単一の真実として参照する（散在させない）。

### 4.2 サーバー側ゲート（重要）

クライアント側の出し分けだけでは改ざん・直叩きで回避可能。段階的にサーバーでも制限する。ゲートは**ソース依存**（SP-APIは各自の枠なので開放、Keepaは共有コストなので制限）。

- アプリはリクエストに `X-App-Plan: free|pro` を付与（Phase 1: 自己申告）。
- サーバーの無料プラン動作:
  - `/api/search`: **SP-API接続時（refresh tokenあり）はSP-API＝オファー同梱を含めて通常どおり返す**（各自の枠のため制限不要）。**SP-API未接続（Keepaフォールバック）時は第1段階の簡易価格のみ**返し、オファーは含めない。
  - `/api/offers`: **Keepa経路（`source=keepa`）は無料に 403（`plan_required`）**。SP-APIのオファーは第1段階同梱のためこのエンドポイント自体を無料が呼ぶ必要がない（アプリは既にsource=spapi時に呼ばない実装）。
  - `/api/graph`: 無料は 403（`plan_required`）。※グラフはKeepa鍵消費＝共有コストのためPro限定。
  - Keepa経路のキャッシュTTLを長めに（未接続の無料はやや古い価格で許容し、サーバーのKeepaコストを削減）。
- Phase 2 で自己申告を廃し、**App Store Server API によるレシート/トランザクション検証**をサーバーで行う（署名付きトークンをアプリへ返し、以降のAPIはそのトークンで判定）。

### 4.2b SP-API認証情報（BYO / OAuth）の保存方針

- 利用者は自分のセラーアカウントを **OAuth（LWA）で接続**し、取得した refresh token をアプリが保持、リクエストヘッダー `X-Spapi-Refresh-Token` で送る（clientId/clientSecret は開発者アプリ共通=サーバー .env）。→ 現行実装済み。
- **公開前に、試験用の「手動SP-APIキー入力欄」は削除**し、接続導線はOAuthのみにする。

#### 保存方式は2段階で考える

**Stage A: 端末セキュア保存（当面の推奨・低コスト）**
- refresh token は現状 `UserDefaults` 保存（`SettingsStore.swift`）だが、セラーアカウントのアクセス権を持つ機微情報のため、**端末のセキュアストレージへ移行する**。
  - iOS: **Keychain**（`kSecClassGenericPassword` / `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` / 非同期＝Synchronizable=false）。
  - Android（将来）: **Android Keystore で生成した鍵で暗号化 → EncryptedSharedPreferences もしくは DataStore+Tink に保存**（`security-crypto` はメンテ停滞のため Tink 直利用を優先検討）。`setUnlockedDeviceRequired(true)`、端末バインド。
- 受け渡し経路も可能なら **ASWebAuthenticationSession（iOS）/ Custom Tabs（Android）** を使い、カスタムURLスキームのクエリでトークンを渡さない設計にする。
- iOS単独 / 少数ユーザーの段階はこれで十分。

**Stage B: サーバー保管＋セッショントークン（マルチプラットフォーム本格化時に移行）**
- refresh token を**端末に置かずサーバーに暗号化保存**し、アプリには**失効可能・権限限定のセッショントークン**のみ渡す（端末には引き続きセッショントークンは保存するが、マスター資格情報ではない＝漏洩時の被害限定＋サーバー側で即失効可）。
- 必要になるもの:
  - **識別＋セッション管理**（必ずしもID/PWログインではない。最小は匿名デバイスID＋アプリ発行セッション。**機種変更/再インストール復元や iOS↔Android の連携共有**が要るなら **Sign in with Apple / Google Sign-In** を導入）。
  - サーバーに**ユーザー識別＋永続ストレージ（DB／暗号化保存）**（現状の依存ゼロ・インメモリ構成には無いので新設）。
  - **セッション期限切れ処理**（401→黙ってセッション再発行→元リクエストを自動リトライ→再発行も失敗なら再認証誘導）。
  - **OAuth完了の冪等化**（連携中に通信断でも「トークン保存済みだがセッション未受領」等の中途半端が残らないよう再開可能に）。
- 通信面の注意: 本アプリは元々検索が全面サーバー依存（オフライン不可）で、Stage B でも**追加ホップは無し**（同一サーバーが認証＋API担当）。新たな失敗モードはセッション期限切れのみで、上記リトライ設計で吸収できる。

#### 移行トリガー（このいずれかで Stage A → B へ）
- Android版を本格展開し、Pro/連携状態を iOS↔Android で共有したくなった。
- 機種変更・再インストール後に Amazon 再連携なしで復元する体験を提供したくなった。
- セラー資格情報の端末保存リスクを完全に無くしたくなった。

### 4.2c 端末証明（App Attest）— 将来実装予定

無料枠は `X-Device-Id` で数えているが、これはクライアントの自己申告に過ぎない。ヘッダーを省略する経路は塞ぎ（2026-08-05、無ければ400）、共有Keepaキーを消費する経路にはIP単位のレート制限も入れたが、**curl等から端末IDを回されると新規の枠を取り放題である点は未解決**。アプリ側の検索クールダウンはSwift実装なのでcurlには一切効かない。

これを塞ぐ本命が Apple の **App Attest**（`DCAppAttestService`）。インストールごとにSecure Enclave内の鍵ペアを作り、Appleのサーバーが署名したattestationでサーバー側が「本物のApp ID・本物のApple端末・改ざんされていないアプリからのリクエスト」であることを検証できる。攻撃の単価が「無料」から「実機の台数」へ跳ね上がる。

**App Attestで解決しないこと（重要）**:
- 鍵は**インストール単位**。アプリを削除して再インストールすれば新しい鍵＝新しい枠になる（＝再インストールによる枠リセットは防げない）。これを塞ぐには `DCDevice`（DeviceCheck、端末ごとに2bitをApple側へ保存。初期化しても残る）が別途必要。
- 脱獄端末で正規の鍵に任意のリクエストを署名させる攻撃は防げない（ただし実機1台ぶんの枠に縛られる）。
- **Pro偽装（自己申告の `X-App-Plan: pro`）は塞げない**。これはApp Store Server APIによるレシート検証側の課題（上記4.2のPhase 2）。両方を組み合わせて初めて塞がる。

**実装方針（着手時の指針）**:
- サーバーはCloudflare Workers。検証にはCBOR/COSE解析・X.509チェーン検証・P-256署名検証が必要。`nodejs_compat` があるため既存npmパッケージが動く可能性は高いが未実証。動かなければ `crypto.subtle` ＋ CBORデコーダの自前実装になる。
- 呼び出しパターンは**セッショントークン方式**を推奨。起動時／日次に1回だけassertionを検証し、keyIdを埋め込んだ短命の署名付きトークン（HMAC）を発行、以降のリクエストはそれを提示するだけにする。毎リクエストにassertionを付けるとRTTとSecure Enclave署名の負荷が乗り、iOS側の10秒タイムアウト予算を圧迫する。
- 識別子は `X-Device-Id` を App Attest の `keyId` に置き換えるのが素直。その際、**AdMob SSVの `custom_data`（`RewardedAdManager` の `customRewardString`）も必ず同時に変えること**。ここがずれると広告視聴の付与先と検索で消費する枠が別デバイス扱いになり、視聴しても枠が増えない。
- keyIdごとの状態（公開鍵＋リプレイ防止カウンタ）はDurable Objectへ。既存の `DeviceQuotaDO` と統合すると、クォータと同じDOで読み書きできてラウンドトリップが減る。
- **シミュレータでは動作しない**（`DCAppAttestService.isSupported` が false）ため、開発・検証は実機必須。ここがコストの大半を占める。
- 鍵は端末移行・再インストール・まれにOS更新で無効化される。`DCError.invalidKey` 等を検知して**再アテステーションへフォールバックする経路が必須**。ここを作り込まないと正規ユーザーが「検索できない」状態で詰む。

**着手判断の目安**: 実装は現実的に1〜2週間規模。リリース後、`KeepaThrottleDO` の shedding（`keepa_busy` 返却）が恒常的に増えている＝正規ユーザーが実際に締め出されていることが観測されてから着手するのが費用対効果に合う。それ以前に優先すべきは上記4.2のレシート検証（Pro偽装は収益に直結するため）。

### 4.2d アカウント（ログイン）— 将来実装予定

**現状**: アカウントは無い。利用者は匿名で使い、Pro状態はStoreKit（同一Apple IDでの「購入を復元」）、仕入れリスト・検索履歴は端末ローカル（`Documents`配下のJSON）、SP-API/Keepaの認証情報はKeychain（上記4.2bのStage A）で持っている。

**きっかけ（2026-08-07）**: 競合の「アマサーチ」「Amacode」がログイン機能を備えている理由を検討した。**Amacodeが iOS と Android の両方で提供されていることは確認済み**。以下のうち1番はそれが根拠で、残りはこの種のアプリが通常必要とするものからの推論（競合の内部事情を確認したわけではない）。

1. **マルチプラットフォーム**（最も効く理由）。StoreKitの「購入を復元」は同一Apple ID内のiOSでしか効かず、Google Playとは別枠。アカウント無しではiOS↔AndroidでPro状態も履歴も引き継げない。
2. **業務データの引き継ぎ**。仕入れリスト・検索履歴は業務データで、失うと実害がある。現状はiCloudバックアップ頼みで、別Apple IDや別OSへは移せない。
3. **無料枠の不正利用対策**。デバイスIDは自己申告なので再インストールやID偽装でリセットできる（4.2c参照）。メール認証付きアカウントなら「タダで無限に増やす」コストが上がる。
4. **サーバー側でしか作れない機能**。価格アラートのサーバー監視＋プッシュ、複数端末での仕入れリスト共有、スタッフとの分担（仕入れ担当と出品担当）。
5. **サポート・返金・プラン変更の同定**。「課金したのに反映されない」の問い合わせでユーザーを特定できる。

**このアプリに現時点で当てはまるもの**:

| 理由 | 現状 |
|---|---|
| マルチプラットフォーム | 該当せず（iOSのみ） |
| データ引き継ぎ | 半分該当（iCloudバックアップで復元されうるが確実ではない） |
| 無料枠の不正対策 | **該当**（4.2cで先送りした課題そのもの） |
| サーバー側機能 | 該当せず（価格アラートは端末内判定、チーム機能なし） |
| サポート同定 | 該当せず（未リリース） |

**方針**:

- **最初からログイン必須にはしない。** このアプリの価値は「バーコードをかざしてすぐ結果が出る」ことで、起動直後にログイン壁を置くと試す前に離脱される。競合は既にブランドと実績があるため許容できるが、未リリースのアプリが同じことをすると不利に働く。
- 導入するなら **Sign in with Apple を任意で追加**する（1タップで済み、メールアドレスも隠せる）。匿名のまま使い続けられる状態を保つ。
- 無料枠の不正対策としてログインを効かせたい場合は、「ログイン無しはお試し数回まで、無料枠を使うにはログイン必須」という形にすれば、初回体験を壊さずに効果を出せる。ただし**任意ログインのままでは不正対策として効かない**（不正者はログインしない）点に注意。その場合は4.2cのApp Attest／DeviceCheck側で対処する。

**4.2b / 4.2c との関係**: 3つは重なる。4.2bのStage B（サーバー保管＋セッショントークン）は「識別＋セッション管理」を必要とし、その最小形は匿名デバイスID、本格形がここで言うアカウント。4.2cのApp Attestは「本物の端末・本物のアプリか」を証明するもので、アカウントの「誰か」とは別の軸。**再インストールによる無料枠リセットはApp Attestでも塞げず、アカウント（またはDeviceCheck）が要る**。

**着手判断の目安**: 4.2bの移行トリガー（Android展開／機種変更での復元体験／端末保存リスクの排除）のいずれかに達したとき。または、リリース後に無料枠の不正利用が実測で問題化し、App Attestより先にアカウントで対処する方が早いと判断したとき。優先順位としては、収益に直結する4.2のレシート検証が先。

### 4.2e Durable Objects / Workers 使用量の事前通知 — 将来実装予定

**きっかけ（2026-08-13）**: `DeviceQuotaDO` が `X-Device-Id` の自己申告値をそのまま `idFromName` に使ってDOを起動しており、認証もIPレート制限も無しに叩けば無制限にDOインスタンス（課金・上限消費対象）を生成できる穴が見つかった。`GET /api/quota` / `GET /api/search`（キャッシュヒット経路）/ `GET /api/graph-data`（キャッシュヒット・事前チェック経路）にIP単位のレート制限を追加して塞いだが（同IPからの連打は防げるが、IPを分散されると防ぎきれない）、これは**攻撃を難しくする対策**であって**万一防ぎきれなかったときに気づく仕組み**は別に要る。

**現状の理解（要点）**:
- このプロジェクトはWorkers Freeプラン。SQLiteバックエンドのDurable Objects（`new_sqlite_classes`）は2025年4月からFreeプランでも利用可能で、上限は**ストレージ合計5GB／1日10万リクエスト（UTC 0時リセット）／HTTPリクエスト1回あたりCPU 10ms**。
- CloudflareのBudget Alerts（2026年4月導入、予測課金額がしきい値を超えたらメール通知）は**従量課金アカウント向け**で、Freeプランのハード上限（リクエスト数等）には対応しない。調査時点で、Freeプランの「上限に近づいたら事前に知らせる」公式機能は見当たらなかった。
- Freeプランでは有料プランと違い、**上限到達＝即座に機能停止**（課金が増えるだけでは済まない）。1日10万リクエストという枠自体は現状の規模なら十分余裕があるが、悪意ある攻撃を想定すると、上限に達した瞬間に正規ユーザーの検索・グラフ取得も含め全機能が止まる。

**選択肢**:
1. **自前で監視の仕組みを作る**: CloudflareのGraphQL Analytics APIを定期的に叩き、当日の使用量を取得。しきい値（例: 日次上限の70%/90%）を超えたらSlack/メール等へ通知する小さな仕組み（別Workerのcron等）を作る。実装可能。
2. **Workers Paidプランへ移行**: 月$5で無料枠が1日10万→月1000万リクエストへ大幅増加。その上でBudget Alertsが使えるようになり、超過時は課金額ベースで事前に気づける（Freeプランのような「気づく前に止まる」リスクが大きく後退する）。

**方針（未確定）**: 上記2つはトレードオフが異なる（1は追加実装コスト、2は月額固定費＋今後の課金額変動）。どちらを選ぶか、または両方（Paid化した上でさらに自前監視も入れる）にするかは未決定。

**着手判断の目安**: 実際に不審なトラフィック増加が観測された、または公開後にDO/KV等の日次使用量が実測でFreeプラン上限に近づいてきたとき。それ以前は優先度低（他の未解決課題の方が公開への影響が大きい）。

### 4.2f SP-API認可の受け渡しをカスタムURLスキームから置き換える — 将来実装予定

**きっかけ（2026-08-21）**: 審査返信のためにOAuthの実装を読み返して気づいた。認可完了後、サーバー（`server/src/oauth.js`）はリフレッシュトークンを**カスタムURLスキームのクエリパラメータ**でアプリへ渡している。

```
barcodesedori://spapi-auth?refresh_token=<平文>&selling_partner_id=...
```

**何が問題か**: カスタムURLスキームは**どのアプリでも同じ文字列を宣言できる**。同一端末に `barcodesedori://` を宣言した別アプリが入っていると、iOSはどちらへ配送するかを保証しない（一般には先にインストールされた方が優先されるが仕様として保証されない）。つまり原理的には、悪意あるアプリがリフレッシュトークンを横取りし得る。トークンはユーザーのAmazon出品用アカウントを操作できる資格情報なので、影響は小さくない。

なお同じ画面にあった「手動コピー用のtextareaでトークンを平文表示する」経路は、貼り付け先だったアプリ側の入力欄が既に廃止され案内先が存在しなかったため、**2026-08-21に削除済み**。現在の受け渡し経路はディープリンク1本。

**選択肢**:
1. **Universal Links へ移行**: `apple-app-site-association` を `sellira.jp` に設置し、アプリに Associated Domains entitlement を追加する。ドメイン所有の証明がAppleによって行われるため、他アプリが横取りできない。**コスト**: entitlementは現在`project.yml`に無いため追加が必要 → App IDの機能有効化 → プロビジョニングプロファイル再発行。AASAファイルの設置は**別リポジトリ（sellira-site）**の作業。
2. **短命な引き換えコード方式（PKCE相当）**: ディープリンクでは一度きり・短寿命のコードだけを渡し、アプリがHTTPSでコード→トークンを交換する。横取りされてもコード単体では使えない（PKCEのcode_verifierを併用すれば確実）。**コスト**: サーバーがトークンを一時的に保持する必要が出る。**現在Appleへ「リフレッシュトークンはサーバーに保存しない」と説明しているため、採用するならその説明とプライバシーポリシーの記述を同時に更新すること。**

**方針（未確定）**: 1はインフラ作業が重いが説明が単純（保存が増えない）。2はアプリの再プロビジョニングが不要だが、保存に関する対外的な説明を変える必要がある。

**着手判断の目安**: **App Store審査を通過した後**。審査中はentitlement追加もプロビジョニング変更も新しいビルドを要求し、進行中の審査サイクルを不必要に複雑にする。公開後、落ち着いてから着手する。

### 4.2g スキャン履歴を100件ごとのファイルへ分割する — 将来実装予定

**きっかけ（2026-08-21）**: 履歴が増え続けたときの起動時デコードを心配して調べたところ、実際に効いてくるのは読み込みではなく**書き込み**だと分かった。

**実測（1件あたり約543バイト。Keepa経路・オファーなしの実データから算出）**:

| 件数 | `scan_history.json` |
|---|---|
| 1,000件 | 0.5 MB |
| 5,000件 | 2.6 MB |
| 20,000件 | 10.4 MB |

※ SP-API連携時は `offersResult`（出品者一覧）が入るためこれより大きい。
※ `imageUrl` はURL文字列のみでファイルには画像データを持たない。

**本当の問題は書き込み**: `ScanHistoryStore.add()` は1件追加するたびに `save()` を呼び、**全件をエンコードしてファイル全体を書き直す**。5,000件たまった状態では1スキャンごとに2.6 MBの書き直しになる。Amazon連携済みならクールダウンは1秒なので、店舗での連続スキャン中は毎秒この処理が走る。エンコードはメインスレッドで実行されるため、スキャンのたびに引っかかりが出うる。件数が増えるほど悪化する。

一方、起動時のデコードは5,000件で2.6 MB＝数十〜百ミリ秒程度で、1日100件スキャンしても5,000件に達するのに50日かかる。**優先すべきは書き込み側**。

**方針**: 履歴を100件ごとのチャンクファイルへ分割する。新しいスキャンは最新チャンクにだけ追記するため、書き直す量が**最大54 KBで一定**になる。表示は最新チャンクから読み、末尾までスクロールしたら次のチャンクを読む。

**設計上の論点**:

1. **検索は全チャンクを読む必要がある**。商品タブには既に検索機能がある（`ProductsTabView.filteredItems`。タイトル・JAN・日付の部分一致）。素直に実装すると検索時に全チャンクを読むことになり分割の意味が薄れる。対策は「A: 検索時は全チャンクを読む（単純。5,000件で2.6 MBなら許容できる可能性）」か「B: タイトル・JAN・日付＋チャンク番号だけの軽量索引をメモリに持つ（1件100バイト程度）」。**Aで始めて遅ければBへ**が現実的。
2. **個別削除でチャンクが歯抜けになる**。商品タブには選択削除がある。中間チャンクから1件消すと99件になるが、**詰め直しはせず「最大100件」と緩く扱う**（読み込みは順に足すだけなので機能上の問題は無い）。
3. **既存ファイルの移行**。現行の単一 `scan_history.json` を初回起動時に分割する。一度きりの処理だが、失敗時に履歴を失わないよう注意する。

**分割せずにできる緩和（先に入れてもよい）**: `save()` を非同期化してメインスレッドから外す、連続スキャン中は書き込みをまとめる（デバウンス）。影響範囲が小さいため、分割より先に入れる選択肢がある。

**着手判断の目安**: **App Store審査を通過した後**。履歴の保存形式変更は保存・読み込み・削除・検索・移行に波及し、影響範囲が広い。現時点の実測では起動時デコードはまだ問題にならない水準なので、再提出を優先してよい。

### 4.3 スキャン制限

- `ScannerView` の `emitCooldown`（現行 1.0 秒固定）を外から注入可能にし、無料=5.0秒 / Pro=1.0秒。
- クールダウン中に読み取った場合は「あと◯秒」の小さなオーバーレイ表示（無反応だと故障に見えるため）。
- **日次上限: 無料は100件/日**。クライアントでカウント（日付が変わればリセット、タイムゾーンはデバイスローカル）し、超過時はペイウォール誘導。サーバーでもデバイスID単位のレート制限をバックストップとして入れる（Phase 3）。

### 4.4 広告

- グラフ枠に AdMob バナー（アダプティブバナー）を表示。Pro は非表示＋実グラフ。
- 必要対応: AdMob SDK 導入（XcodeGen `project.yml` に依存追加）、ATT（トラッキング許可）ダイアログ、`PrivacyInfo.xcprivacy`（プライバシーマニフェスト）、App Store 審査のデータ収集申告。
- 注意: 本アプリ初の外部SDK依存。広告なし構成（ロック表示のみ）でまずリリースし、広告を後続フェーズに回す選択肢もある。

## 5. 変更箇所一覧（ファイル別）

### iOS

| ファイル | 変更 |
|---|---|
| `EntitlementStore.swift`（新規） | StoreKit 2 でPro判定を一元管理 |
| `PaywallView.swift`（新規） | 課金訴求画面（機能比較・購入・復元） |
| `ScannerView.swift` | `emitCooldown` を注入化（free=5s）。クールダウン残り表示 |
| `ScanQuotaStore.swift`（新規） | 日次スキャンカウント（無料100件/日、日付でリセット）。超過時はペイウォール誘導 |
| `SearchTabView.swift` | OCRトグルをロック（タップ→ペイウォール）。オファーパネル: **SP-API接続時は実オファー表示（無料も可）／Keepaのみ(未接続)の無料はぼかしダミー＋鍵**（第2段階を取得しない、タップでペイウォール）。グラフ枠: 無料はぼかし+鍵+広告 |
| `APIClient.swift` | `X-App-Plan` ヘッダー付与。403 `plan_required` のハンドリング |
| `SettingsView.swift` | プラン表示・購入/復元導線。**試験用のSP-API手動キー入力欄を削除**（接続はOAuthのみ） |
| `SettingsStore.swift` | （推奨・非ブロッカー）refresh token を UserDefaults → **Keychain** へ移行 |
| `project.yml` | AdMob SDK 追加（広告フェーズ時）→ `xcodegen generate` 必要 |

### サーバー

| ファイル | 変更 |
|---|---|
| `src/routes.js` | プラン判定（ヘッダー→Phase 2でトークン検証）。無料: **SP-API接続時は通常どおり（オファー同梱含む）／Keepaフォールバック時は簡易価格のみ**、`/api/offers`(source=keepa)・`/api/graph`=403、Keepaキャッシュ TTL 延長 |
| `src/entitlement.js`（新規, Phase 2） | App Store Server API 検証 |
| `test/` | プランゲートのテスト追加（ソース依存ゲート: SP-APIは開放・Keepaは制限） |

## 6. 実装フェーズ

- **Phase 1（クライアント課金 + 自己申告ゲート）**
  1. StoreKit 2 / EntitlementStore / ペイウォール（¥1,980/月・3日トライアル・復元）
  2. クライアント側ゲート（OCRロック・5秒クールダウン・日次100件・オファーぼかしダミー＋鍵・グラフロック表示）
  3. サーバー側ゲート（`X-App-Plan` ベース・ソース依存: SP-APIは開放／Keepaの offers・graph は 403）
  4. テスト（サーバー）+ 実機確認 → リリース
- **Phase 2（広告）**
  5. AdMob 導入（ATT・プライバシーマニフェスト・審査申告）、グラフ枠へバナー表示
- **Phase 3（不正対策強化）**
  6. App Store Server API によるサーバー側レシート検証（自己申告ヘッダー廃止）
  7. サーバー側のデバイス単位レート制限（日次上限のバックストップ）

## 7. リスク・注意

- **審査**: サブスクは「機能比較・価格・解約方法」の明記が必要。トライアル文言も規約に沿うこと。
- **広告SDK**: ATT許可率次第で収益が変動。導入コスト（SDK・審査対応）に対し、少DAU期はPro課金の方が費用対効果が高い可能性 → 広告をPhase 2に分離した理由。
- **無料のKeepa消費**: SP-API未接続の無料ユーザー増加はKeepaトークン消費に直結。日次上限とKeepaキャッシュTTL延長を同時に入れるのが安全（SP-API接続者は各自の枠なので対象外）。
- **SP-APIレート枠**: BYO（セラー単位）のため利用者間で干渉しない。共有コストにならない。
- **既存ユーザー**: 現行TestFlight/自分用ビルドはすべてPro相当の動作。公開時の初期プラン扱いを決めること。

## 8. 決定事項（2026-07-14 確定）

1. 価格: **¥1,980/月**、トライアル: **3日間**
2. 日次スキャン上限: **入れる。無料100件/日**（Proは無制限）
3. **SP-API接続時（BYO）は無料でもオファー一覧を実表示**（第1段階同梱＝追加コストなし、各自の枠）。**Keepaのみ（未接続）の無料は非表示**（鍵＋ぼかしダミー、実取得なし＝トークン消費ゼロ）
4. **SP-APIはOAuth(BYO)接続**。公開時に試験用の手動キー入力欄は削除。refresh token の Keychain 移行は推奨（非ブロッカー、開発者判断）
5. 損益分岐点（breakEven）: **無料に含めてよい**（SP-API接続時、各自の枠で算出＝共有コストなし）
6. 履歴保存制限: **なし**（無料も無制限）
7. 広告: **入れる**（AdMob、Phase 2）
8. 買い切り: **なし**（サブスクのみ）
9. Keepaグラフ: **Pro限定**（Keepa鍵消費＝共有コストのため）
10. OCR: **無料は1日3回までお試し可**（`ScanQuotaStore.freeOcrDailyLimit=3`）。超過で鍵→ペイウォール。読み取り成立ごとに消費。
11. オファーのロック文言は「**設定→Amazon連携で表示**」。タップで設定タブへ誘導。
12. **オファー一覧はSP-API連携必須（Proでも連携無しなら非表示）**。Keepa第2段階=getProduct(offers)のトークン消費を避けるため、Keepa経路ではオファーを実取得せずロックする（無料/Pro共通）。Keepaグラフ・OCR等の他のPro特典はそのまま。
