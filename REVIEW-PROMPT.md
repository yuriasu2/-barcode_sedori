# レビュー依頼が表示される条件

実装: `ios/BarcodeSedori/Sources/Store/ReviewPromptController.swift`

## 出すタイミング(トリガー)

| トリガー | 場所 | 対象 |
|---|---|---|
| 主: 一括出品が全件成功 | `PurchaseTabView` | SP-API連携済みユーザー |
| 副: 起動時(2.5秒後、操作中でなければ) | `SearchTabView` | 全ユーザー(無料/Keepa-BYOはこちらのみ届く) |

## 出す条件(すべて満たす必要あり)

1. **利用バー**
   - 累計検索回数 50回以上
   - 起動した日数(暦日) 3日以上
   - かつ「仕入れリスト追加5件以上」または「一括出品成功1回以上」のどちらか
2. **直近5分以内にネガティブイベントが無い**
   - エラー・検索失敗・枠切れ・ペイウォール表示・リワード広告視聴、いずれも該当
3. **前回の依頼から120日以上経過**
4. **直近365日以内の依頼が2回未満**(Appleの上限3回/365日より手前で止める)
5. **同一アプリバージョンで未依頼**

## 注意点

- アプリ側は「依頼した」ことしか記録できない。実際にダイアログが出たかはiOS側の裁量(出ないこともある)。
- 設定タブの「レビューを書く」ボタンは上記と無関係。App Storeのレビューページへ直接リンクするだけで、システムダイアログは呼ばない(Appleガイドラインでボタンから`requestReview`を呼ぶのは禁止のため)。
- `AppStoreReviewConfig.appId`(`SettingsView.swift`)が空の間は上記ボタンごと非表示。**公開前に設定が必要**(`FREEMIUM-PLAN.md`の「公開前 必須TODO」参照)。
- 各条件の数値は`ReviewPromptController.swift`内の`enum Policy`にまとまっており、そこだけ見れば調整できる。
