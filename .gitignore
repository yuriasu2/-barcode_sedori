# 機密情報(server/.gitignoreでも除外済みだが念のため)
.env
server/.env

# App Store Connect APIの秘密鍵。Appleは再ダウンロードを許さないため紛失すると
# キーの再発行が必要になる。リポジトリには絶対に置かない(tools/appstore-connect/README.md参照)。
*.p8
AuthKey_*.p8

# Node
node_modules/

# Xcode / XcodeGen生成物
*.xcodeproj
xcuserdata/
DerivedData/
build/

# macOS
.DS_Store
