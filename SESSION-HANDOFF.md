# セッション引き継ぎ(2026-08-05 時点)

アプリ名: **アマレンズ**(旧「バーコードせどり」)。Amazonセラー向けの仕入れリサーチiPhoneアプリ。**まだ一般公開前**(App Store/Amazon掲載とも未申請)。

## 関連リポジトリ

| 対象 | 場所 | デプロイ |
|---|---|---|
| iOSアプリ＋サーバー | このディレクトリ / `yuriasu2/-barcode_sedori` | サーバーは**手動** `cd server && npx wrangler deploy`(= `npm run deploy:cf`) |
| LP(sellira.jp) | `~/Claude/Projects/sellira-site` / `yuriasu2/sellira-site` | push→Cloudflare Pages が自動 |

両リポジトリとも作業ツリーはクリーン・push済み。

## 直近の作業(完了・本番デプロイ済み)

**Keepaスロットルのキュー撤去 + リクエスト・コアレッシング導入**(コミット `3feecba`〜`07a4962`、計14コミット)

- 枯渇時に最大6秒待たせるキュー機構を撤去。`acquire`は即時許可/即時拒否(`reason: 'exhausted'`)の2択に。5トークン/分の現プランでは6秒待っても補充が間に合わず、しかもDurable Objects環境でキューの自動実行タイマーが確実に発火しない問題があったため。
- 代わりに単一フライト方式のリクエスト・コアレッシング(`server/src/keepaCoalesce.js`)を新規実装し、`/api/search`・`/api/graph-data`に統合。同じ商品への同時リクエストを1回のKeepa呼び出しにまとめる。
- 適応ブレーキ(消費速度に応じた予防的遅延)は維持。上限は独立した`BRAKE_CAP_MS=4000ms`定数に。
- Pro優先は「キュー内の順序」から「適応ブレーキの安全圏の差(Pro=2分/無料=10分)＋コアレッシングキーへの優先度組み込み」に置き換え。
- コアレッシング導入でKeepa呼び出しがユニット消費より前に来るため、無料枠の非消費事前チェックを追加(残量ゼロなら呼ばずに拒否)。
- iOS側: キュー可視化専用だった開発者向けUI(「同時リクエストをテスト」)とモデルフィールド(`queueLength`/`depth`)を撤去。
- 設計書`docs/superpowers/specs/2026-08-02-keepa-token-depletion-design.md`の§8に改訂記録を追記。

**実装過程で見つかり修正した重大バグ(要知識として残す)**:
1. BYOキー(自分のKeepa契約)利用者同士がコアレッシングで束ねられ、他人の課金枠で処理される不具合(Critical)。APIキーのSHA256ハッシュ先頭8桁を単位キーに含めて分離。
2. Proが無料ユーザーの後ろに束ねられ、無料向けの適応ブレーキを巻き添えで受ける不具合(Important)。優先度を単位キーに含めて分離。

サーバーテスト **295/295パス**。Subagent駆動(実装→レビュー→修正→再レビュー)で1タスクずつ進め、最終的に全体ブランチレビューも実施し「修正後にShip」の指摘を全て対応済み。

**本番(`api.sellira.jp`)へは既にデプロイ済み**(2026-08-05、ユーザー承認の上で実行)。ただしアプリ自体はまだ一般公開前のため実害は限定的。

## 次にやること(優先度順)

### 1.【新規・要調査】アプリ名「アマレンズ」と競合1位アプリ「アマサーチ」の類似性

2026-08-05にユーザーから懸念提起。同一カテゴリ(せどりリサーチ・Amazon出品者向けバーコードスキャン)・同一ターゲット層の人気アプリ(評価4.7、App Store掲載あり)と名称が似ている。

- 「アマ」接頭辞はこの業界で一般的(他に「アマコード」等も存在)なので、それだけでは決定打になりにくいと考えられるが、カテゴリ・販路が完全一致している点はリスク要因。
- Web検索では「アマサーチ」の商標登録有無を確認できなかった(J-PlatPatは対話的検索が必要で自動取得不可)。
- **次のアクション**: ユーザー自身がJ-PlatPat(https://www.j-platpat.inpit.go.jp/)で「アマサーチ」を称呼検索し登録有無・区分・権利者を確認。App Store申請前に弁理士への相談も推奨(初回無料相談を行う事務所が多い)。
- Amazon掲載申請(下記2番)を進める前に、この点の結論を待つべきか要確認。

### 2.【要対応】未解決の既知課題: デモエンドポイントの認証なし公開

サブタスクとして切り出し済み(chip: `task_afbdd052`)。`POST /api/keepa-throttle-demo/seed`・`/probe`が無認証で本番稼働中。一度seedされるとDurable Objectのストレージ書き込みが走るため、無認証ループで無料枠書き込み上限を消費し得る。本番の`global`インスタンスは影響を受けない。一般公開前の対応を推奨。

### 3. Amazon 販売パートナーアプリストアの掲載申請

Solution Provider Portal のフォームを記入中。記入案は `APPSTORE-LISTING-DRAFT.md`。

- 製品URL: `https://sellira.jp/amalens/` ／ サポートURL: `https://sellira.jp/support/`
- サポートEメール: `saastids2025@gmail.com` ／ 電話は**空欄推奨**(Amazonがテスト架電する可能性)
- 出品カテゴリー: `商品調査およびスカウト`／機能: `商品調査およびスカウト`＋`分析とレポート`
- **サポートするプログラムは未選択**(下記4の課題があるためFBA/MFNどちらにもチェックしない)
- 価格設定モデルは「無料」を選び、説明欄でProプラン(月額1980円・3日間無料体験)を明記
- ロゴは `design/amalens-logo-1024.png` を詳細ページ用・カテゴリタイル用の両方に使う
- フォーム内は半角の `" ' & < > $` が使用不可
- 掲載内容は**実装済み機能のみ**とする方針(公開後もEdit Listingで追記可能)
- **→ 上記1番(アプリ名の懸念)の結論が出るまで申請を保留する判断もあり得る**

### 4.【要対応】損益分岐点がFBA固定になっている

`server/src/spapi/pricing.js` の `IsAmazonFulfilled: true` がハードコードで、切り替え手段がない。設計上の出品機能は**自己発送(MFN)のみ**なので、自己発送セラーには実態と合わない損益分岐点が出る。設定でFBA/自己発送を切り替えるのが本筋。

### 5. 出品機能・利益アラートの実装

設計書: `docs/superpowers/specs/2026-07-19-listing-and-profit-alert-design.md`(設計承認済み・実装未着手)

- Phase 1: 利益アラート(損益分岐点が閾値以上で強調)＋仕入れリスト
- Phase 2: アプリ内出品(自己発送のみ、Listings Items API)
- Product Listing ロールは承認済み。ただし**アプリにロールを追加すると既存の連携ユーザー全員が再認可必要**になるため、Phase 2着手時にまとめて行う。

### 6. リリース前に必ず消すもの

- SP-APIリフレッシュトークンの手入力欄(DisclosureGroup内)
- AdMobのテストID → 本番IDへ差し替え、SKAdNetwork全リスト追加
- バンドルID `com.example.barcodesedori` の変更
- 掲載承認後: `server/src/oauth.js` の `version=beta` を外す

### 7. 保留中

- Render の削除(Cloudflare移行済み。`LWA_REFRESH_TOKEN` が残っているので消す)

## 作業ルール(プロジェクト固有)

- 変更完了ごとに確認なしでコミットする
- iOS変更は毎回ビルド＋シミュレータで確認(ユーザーが「検証は自分でやる」と言った場合はビルドのみ)
- サーバーは自動デプロイされない。反映するには明示的に `wrangler deploy` が必要。`KEEPA_API_KEY`はCloudflare secretのみに存在しローカル`.env`には無いため、ローカルでKeepa実データ検証はできない
- 要件定義・計画・レビュー・設計相談・複雑なバグ調査はOpus、実装・ファイル整理・単純作業はSonnetのサブエージェントへ委任、質問への回答はSonnet(プロジェクトCLAUDE.mdの指示)
- 本セッションではSubagent駆動開発(実装→レビュー→修正→再レビューを1タスクずつ)を使用。計画書は`docs/superpowers/plans/`、タスクブリーフ・レビュー結果は`.superpowers/sdd/`(gitignore対象)に格納
